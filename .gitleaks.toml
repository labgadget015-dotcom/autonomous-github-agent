title = "Repository gitleaks overrides"

[extend]
useDefault = true

[allowlist]
description = "Ignore known false positives in baseline and API docs examples"
paths = [
  '''^\.secrets\.baseline$''',
  '''(^|.*/)API_EXAMPLES\.md$''',
]
